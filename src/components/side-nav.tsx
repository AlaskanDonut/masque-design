import { ModeToggle } from "./mode-toggle";

export default function SideNav() {
  return (
    <nav className="flex flex-col gap-y-3 w-full h-full">
      SideNav
      <ModeToggle />
    </nav>
  );
}
